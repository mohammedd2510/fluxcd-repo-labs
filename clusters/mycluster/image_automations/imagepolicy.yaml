apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: react-app-cicd
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: react-app-cicd
  policy:
    semver:
      range: ">=0.0.1"
